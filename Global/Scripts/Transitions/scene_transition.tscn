[gd_scene load_steps=4 format=3 uid="uid://ve01x6lhoa4s"]

[ext_resource type="Script" uid="uid://d2lqvwefbgyko" path="res://Global/Scripts/Transitions/scene_transition.gd" id="1_jlyoa"]

[sub_resource type="Shader" id="Shader_pmnff"]
code = "shader_type canvas_item;

// Ranges from 0 to 1 over the course of the transition.
// We use this to actually animate the shader.
uniform float progress : hint_range(0, 1);

// Size of each diamond, in pixels.
uniform float diamondPixelSize = 10.0;

void fragment() {
	float xFraction = fract(FRAGCOORD.x / diamondPixelSize);
	float yFraction = fract(FRAGCOORD.y / diamondPixelSize);
	float xDistance = abs(xFraction - 0.5);
	float yDistance = abs(yFraction - 0.5);
	if (xDistance + yDistance + UV.x + UV.y > progress * 4.0) {
		discard;
	}
}"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_3gfpi"]
shader = SubResource("Shader_pmnff")
shader_parameter/progress = 1.0
shader_parameter/diamondPixelSize = 10.0

[node name="SceneTransition" type="CanvasLayer"]
script = ExtResource("1_jlyoa")

[node name="Background" type="TextureRect" parent="."]
material = SubResource("ShaderMaterial_3gfpi")
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
grow_horizontal = 2
grow_vertical = 2
mouse_filter = 2
